import { StyleSheet } from 'react-native'

export const ss = StyleSheet.create({
    container: {
        // flex: 1,
    },
    image: {
        height: 64,
        width: 64,
        borderRadius: 8,
        marginRight: 20
    }
})